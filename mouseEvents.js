function initPage(){
    let catachEventsDiv = document.getElementById("catchEventsDiv");
    catachEventsDiv.addEventListener("mousemove",   (event)=>{
        console.log(event);
    });
    catachEventsDiv.addEventListener("mousedown",   (event)=>{
        console.log(event);
    });
    catachEventsDiv.addEventListener("onkeypress",   (event)=>{
        console.log(event);
    });
    catachEventsDiv.addEventListener("onkeyup",   (event)=>{
        console.log(event);
    });
    catachEventsDiv.addEventListener("mouseover",   (event)=>{
        console.log(event);
    });
    catachEventsDiv.addEventListener("onclick",   (event)=>{
        console.log(event);
    });
    catachEventsDiv.addEventListener("mouseup",   (event)=>{
        console.log(event);
    });
    catachEventsDiv.addEventListener("mouseout",   (event)=>{
        console.log(event);
    });
    catachEventsDiv.addEventListener("dblclick",   (event)=>{
        console.log(event);
    });
    catachEventsDiv.addEventListener("contextmenu",   (event)=>{
        console.log(event);
    });
}




